chainlit>=2.6
fastapi
uvicorn[standard]
websockets
langgraph>=0.6
langchain-core
# Pinned: `thinking` (extended thinking) must be a real constructor field. On a build
# without it the kwarg is NOT rejected - pydantic silently shunts it into model_kwargs
# with a UserWarning, so make_model()'s `except TypeError` never fires and the turn
# fails later at the API call instead. Floor guarantees the field; <2 stops a major bump.
langchain-anthropic>=1.7.2,<2
langchain-openai
openai>=1.0
langgraph-checkpoint-postgres
langgraph-checkpoint-sqlite
psycopg[binary,pool]
sqlalchemy[asyncio]
asyncpg
aiosqlite
bcrypt
python-dotenv
httpx
beautifulsoup4
lxml
ddgs
google-api-python-client
google-auth
google-auth-httplib2
pypdf
python-docx
cryptography>=42
openpyxl
python-pptx
fpdf2
pillow
playwright
